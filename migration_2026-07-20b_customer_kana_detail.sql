-- ============================================================
-- 村上農園 メロン予約管理 顧客マスタにB列（細かい五十音区分）を追加（2026-07-20）
-- SupabaseのSQL Editorに貼り付けて「Run」を押してください
--
-- 内容：
--   名前選択リストで、B列（あ/い/う/え/お…の細かい区分）ごとに
--   チップの色を変えて見分けやすくするため、customers に
--   kana_detail 列を追加する。
--
-- 注意：この後、更新版の seed_customers.sql を実行して
--       既存データの kana_detail を埋めてください。
-- ============================================================

alter table public.customers
  add column if not exists kana_detail text;

-- ============================================================
-- 完了！
-- ============================================================
