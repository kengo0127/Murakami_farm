# 村上農園 タスク管理Webアプリ 要件定義

## 概要

農園スタッフが日々の農作業タスクをスマートフォンから確認・完了報告し、管理者がタスクを設計・進捗を把握するためのWebアプリケーション。

---

## ユーザー

- **スタッフ（作業員）**：約40名。スマートフォンから操作。
- **管理者**：1〜2名。PCからタスク設計・進捗確認。

---

## 主な機能

### 1. 認証

- メールアドレス＋パスワードによる個人アカウントログイン
- 役割は「スタッフ」と「管理者」の2種類
- ログアウト機能

### 2. スタッフ画面（モバイル優先）

- ログイン後、自分に割り当てられた当日のタスク一覧を表示
- タスクは時系列（タイムライン形式）で表示
- 各タスクに「完了」ボタンを設置
- 完了ボタンは、タスクごとに１つだけなので、スタッフの誰か一人が押す
- 完了ボタンを押すと完了時刻を記録し、次のタスクをハイライト表示
- 各タスクに「開始」ボタンを設置。開始を押すと開始時刻を記録し、完了ボタンに切り替わる
- 開始／完了ボタンの押し間違いに備え、「取消」操作を用意
  - 開始の取消：押した本人のみ、完了報告がされるまでの間だけ可能
  - 完了の取消：押した本人のみ可能
- 自発的な作業報告：スタッフが自主的に行った作業（収穫／整理整頓／除草／在庫確認）を選択式で管理者に報告できる
  - 収穫を選んだ場合は、数量（0〜99の整数）と単位（kg／本／玉）を追加入力
- 昼休憩（10:00〜10:30, 12:00〜13:00、15:00〜15:30）はタイムラインに表示

### 3. 管理者画面（タスク設計）

- 毎朝、その日のタスクをフォームで登録する（当面はシンプルなフォーム入力）
- 入力項目：タスク名、対象作物、担当スタッフ、開始時刻、終了時刻（15分刻み）、メモ
- 登録済みタスクの編集・削除
- 将来的にはOutlookのスケジューラ風のドラッグ＆ドロップUIに発展させる予定

### 4. 管理者画面（進捗確認）

- 作物ごとに縦カラムで分割し、タスクを時系列で表示
- 完了済み・進行中・未着手・遅れ の状態を視覚的に表示（左ボーダーの色分け）
- 現在時刻ラインを表示し、遅れを一目で把握できる
- 他スタッフが完了ボタンを押したらリアルタイムで反映

### 4.5 管理者画面（自発報告）

- 日付ごとに、スタッフからの自発的な作業報告（時刻・スタッフ名・作業内容・数量／単位）を一覧表示
- CSVエクスポート（データ出力タブから、タスク記録とは別ファイルで出力）

### 5. データ記録・エクスポート

- タスクの完了時刻・所要時間をDBに自動記録
- 記録データをCSV／Excelとしてダウンロード可能（Excel用BOM付きUTF-8）
- エクスポート項目（1タスク1行）：

| カラム | 内容 |
|--------|------|
| 日付 | 作業日 |
| スタッフ数 | タスクに割り当てられたスタッフ人数 |
| タスク名 | |
| 作物 | |
| 予定開始 | |
| 予定終了 | |
| 予定所要時間(分) | 予定終了 − 予定開始 |
| 完了時刻 | 実際の完了時刻 |
| 所要時間(分) | 予定開始から完了までの経過時間 |
| 予実の差分(分) | 所要時間 − 予定所要時間（マイナス＝早期完了） |
| スタッフ名 | 割り当てられた全スタッフ名（読点区切り） |

---

## 対象作物（初期）

- スイカ
- メロン

---

## タスクの作業順序（初期設定）

### スイカ（スタッフA, B, C担当）
整枝＋玉直し（7:00〜10:00）→ 水やり（10:30〜12:00）

### メロン（スタッフD,E,F,G担当）
整枝＋玉直し（7:00〜10:00）→ 水やり（10:30〜12:00）

---

## 技術スタック

| 役割 | 採用技術 | 理由 |
|------|----------|------|
| データベース・認証・API | Supabase（PostgreSQL） | 無料枠が十分、SQLでClaudeと開発しやすい、RLSでセキュア |
| フロントエンド | HTML / JavaScript | シンプルで拡張しやすい |
| ホスティング | **GitHub Pages** | 無料・静的ファイルのみで動作 |

- 公開URL：`https://kengo0127.github.io/Murakami_farm/`
- GitHubリポジトリ：`https://github.com/kengo0127/Murakami_farm`

---

## Supabase 設定メモ

- **Confirm email**：オフ（Authentication → Sign In / Providers → Email）
- **メール確認の自動承認トリガー**：`on_auth_user_auto_confirm`（SQL Editorで設定済み）
  - 新規ユーザー追加時に `email_confirmed_at` を自動セット
- **URL Configuration**：GitHub PagesのURLを Site URL・Redirect URLs に登録済み
- アカウントは管理者がSupabaseダッシュボードから一括作成（招待メール不使用）

---

## DBテーブル設計

### users（ユーザー）
Supabase Authが管理。追加情報をprofilesテーブルで管理。

```
profiles
- id (uuid, FK to auth.users)
- name (text) ─ 表示名
- role (text) ─ 'staff' or 'admin'
- created_at (timestamp)
```

### tasks（タスク）
```
tasks
- id (uuid, PK)
- date (date) ─ 作業日
- crop (text) ─ 対象作物
- task_name (text) ─ タスク名
- scheduled_start (time) ─ 予定開始時刻
- scheduled_end (time) ─ 予定終了時刻
- note (text) ─ メモ・補足
- created_by (uuid, FK to profiles) ─ 登録した管理者
- created_at (timestamp)
```

### task_assignments（タスク担当割り当て）
タスクとスタッフの多対多を管理するテーブル。
```
task_assignments
- id (uuid, PK)
- task_id (uuid, FK to tasks)
- staff_id (uuid, FK to profiles) ─ 担当スタッフ
- created_at (timestamp)
```

### task_completions（完了記録）
```
task_completions
- id (uuid, PK)
- task_id (uuid, FK to tasks) ─ UNIQUE（1タスク1件）
- completed_by (uuid, FK to profiles) ─ 完了ボタンを押したスタッフ
- completed_at (timestamp) ─ 実際の完了時刻
- duration_minutes (integer) ─ 予定開始時刻からの経過時間（分）
- note (text) ─ 完了コメント（任意）
```

### task_starts（開始記録）
```
task_starts
- id (uuid, PK)
- task_id (uuid, FK to tasks) ─ UNIQUE（1タスク1件）
- started_by (uuid, FK to profiles) ─ 開始ボタンを押したスタッフ
- started_at (timestamp) ─ 実際の開始時刻
```
本人による取消（DELETE）は、完了記録が存在しない間のみ許可（RLSで制御）。

### work_reports（自発的な作業報告）
```
work_reports
- id (uuid, PK)
- date (date) ─ 報告日
- staff_id (uuid, FK to profiles) ─ 報告したスタッフ
- work_type (text) ─ '収穫' / '整理整頓' / '除草' / '在庫確認'
- quantity (integer, nullable) ─ 収穫時のみ、0〜99
- unit (text, nullable) ─ 収穫時のみ、'kg' / '本' / '玉'
- reported_at (timestamp) ─ 報告時刻
```

### task_delay_notifications（遅延通知の送信済み記録）
```
task_delay_notifications
- task_id (uuid, PK, FK to tasks) ─ 送信済みタスク（1タスク1件）
- notified_at (timestamp) ─ 通知送信時刻
```
Edge Function（サービスロール）専用のテーブル。クライアント向けのRLSポリシーはなし（全拒否）。

---

## 開発方針

- **段階的に開発する**。最初はシンプルなフォーム入力から始め、UIは要件が固まってから作り込む
- **Claude Code を活用**してコードを生成・修正する
- UIの変更が頻繁に発生することを前提に、ロジックとUIを分離して設計する
- スマートフォン対応（レスポンシブ）を最初から意識する

---

## 優先順位（開発順序）

1. ✅ Supabaseのテーブル作成・RLS設定
2. ✅ ログイン／ログアウト機能
3. ✅ スタッフ向けタスク表示画面（モバイル）
4. ✅ 完了ボタン＋記録機能
5. ✅ 管理者向けタスク登録フォーム
6. ✅ 進捗確認画面（管理者）
7. ✅ CSV／Excelエクスポート
8. ✅ GitHub Pages でホスティング・公開
9. スタッフアカウント一括作成機能（管理者画面から）
10. タスク設計UIの改善（スケジューラ風）

---

## 未決定事項

- スタッフへのアカウント配布方法（現在はSupabaseダッシュボードで手動作成）
- 複数日分のタスクをまとめて登録する機能の要否（前日タスクのコピーなど）

## 通知機能（メール）

- **押し忘れ検知メール（実装済み、2026-07-19に休憩前再チェック・開始遅延を追加）**
  - 実現方法：Supabase Edge Function（`supabase/functions/check-delayed-tasks`）を pg_cron で5分おきに自動実行し、Resend経由で管理者へメール送信
  - 以下3種類のチェックを毎回まとめて実行：
    1. **完了遅延通知**：完了予定時刻（scheduled_end）から5分経過しても完了ボタンが押されていない場合に通知
    2. **休憩直前の再チェック通知（新規）**：9:55/11:55/14:55/16:55（休憩開始10:00/12:00/15:00/17:00の5分前）の時点で、完了予定時刻を過ぎているのにまだ完了報告がない場合に通知。5分後通知を見過ごしたスタッフに対し、休憩時間に管理者が対面で完了ボタンの確認・注意ができるようにするための仕組み
    3. **開始遅延通知（新規）**：開始予定時刻（scheduled_start）から10分経過しても開始ボタンが押されていない場合に通知
  - 同じタスク×同じ種類の通知への二重送信を防ぐため、送信済みの組み合わせは `task_delay_notifications` テーブルに `(task_id, notification_type)` の複合主キーで記録（`notification_type`: `completion_delay` / `completion_break_0955`等 / `start_delay`）
  - 送信先は環境変数 `ADMIN_ALERT_EMAIL`（Supabase Edge Functionsのsecrets）で管理。現在は `oshmsakaikufa@gmail.com` 宛
  - **制約**：Resendで独自ドメインを認証登録していないため、Resendアカウント作成時のメールアドレス（`oshmsakaikufa@gmail.com`）以外には送信できない。他の宛先に変更・追加したい場合は、その宛先でResendアカウントを作り直すか、独自ドメインを取得してResendに認証登録する必要がある
  - LINE Notifyは2025年3月末にサービス終了しているため使用不可

## 保留中の機能（今後実装）

- **終了5分前のスタッフ向けリマインドメール**
  - 終了予定時刻の5分前になっても完了ボタンが押されていない場合、担当スタッフ本人のメールアドレスへ「作業終了5分前です」を送信
  - 上記の遅延通知と同じ仕組み（Edge Function + pg_cron）を流用して追加実装できる

---

## インスタ・EC：お客様注文フォーム機能（2026-08-13追加）

- **背景**：これまでインスタDMで受けた注文内容を社員が予約管理表（melon_sheet.html）へ1行ずつ手入力しており、転記の手間が課題だった
- **変更内容**：ログイン不要でアクセスできるお客様向け注文フォーム（`melon_order.html`）を追加。送信内容はその場で予約表に反映せず、いったん「注文リクエスト」として `melon_order_requests` テーブルに保存する
- 社員は `melon_requests.html`（未処理リクエスト一覧）で内容を確認し、「承認」すると自動で `melon_entries`（予約表の行）に登録される。誤りがあれば「却下」も可能
- **セキュリティ**：匿名ユーザー（お客様）はリクエストの insert のみ可能（RLSで select/update/delete を禁止）。単価・箱代など金額に関わる列はリクエストに持たせず、承認時にそのシートの適用単価を自動で設定するため、お客様側で価格を改ざんすることはできない
- 未処理リクエストがある場合、月一覧画面（melon.html）にバッジ付きの案内バナーを表示
- 簡易なbot対策としてハニーポット欄をフォームに設置（reCAPTCHA等の本格的な対策は未導入）
- 関連ファイル：`migration_2026-08-13_melon_order_requests.sql` / `melon_order.html` / `melon_requests.html`
- お客様向けフォームのURLは、村上農園のインスタのプロフィール欄・固定投稿等で案内する想定
- 承認・却下の画面（melon_requests.html）には、連絡先をワンタップでコピーできるボタンを追加（承認・却下いずれの場合もお客様への連絡が必要なため）

### 表示・名称の変更（2026-08-13追加）

- 「メロン予約」の名称を「インスタ・EC」に統一（今後スイカなど他品目も同じ仕組みで扱う想定のため）。admin.htmlのタブ、各画面のタイトル・見出し、CSVファイル名を変更
- 月一覧画面（melon.html）を、月カードのサムネイル表示から、対象月をテーブルの行として追加していく形式に変更（上部に月選択＋「行を追加」ボタンを常設）
- 予約管理表（melon_sheet.html）の表示を改善：
  - 帳簿本体のスクロールバーを常時太く表示（OS設定でオーバーレイ表示になり見落とされていたため）
  - 「単価一覧」「当日・前日の出荷状況」パネルをクリックで開閉できる折りたたみ式にし、デフォルトで閉じておくことで帳簿の表示スペースを確保

### お客様向けフォームの項目調整（2026-08-13追加）

- お客様向けフォーム（melon_order.html）からも「メロン」の全体表記を削除（今後スイカ等も扱うため）。ただし注文内容セクションの見出しは「メロン ご注文内容」とし、3L/4L/5L等がメロンの規格であることが分かるようにした
- 「お届け希望日」を「ご来店希望日」に変更（配送ではなく来店・対面での受け渡しのため。melon_requests.html側の表示も統一）
- ご連絡先を必須項目に変更し、注記に「メールアドレス」を追加（承認・却下いずれの場合もお客様への連絡が必須のため）
- ご来店希望日は本日以降のみ選択可能（`min`属性＋JS側バリデーションで過去日を拒否）
- 使われていなかった「その他 数量」「その他 内容」の入力欄を削除（`melon_entries` / `melon_order_requests` の `qty_other` / `other_label` カラム自体は残置）
- 送信完了メッセージを「担当者が内容を確認のうえ、ご連絡いたします」に変更（予約リストへの登録を前提とした表現をやめた）

### 注文リクエストのLINE通知（2026-08-13追加）

- タスクの押し忘れ通知（`check-delayed-tasks`）と同じ、管理者向けLINE公式アカウントの仕組みを流用
- 実現方法：新規Edge Function `supabase/functions/notify-order-requests` を pg_cron で5分おきに自動実行
- 以下2種類の通知を行う：
  1. **新規受信通知**：新しい注文リクエストが届いたら、数分以内にLINEで通知
  2. **未対応の催促通知**：`pending`状態のまま受付から60分経過しても未対応の場合に再通知
- 二重送信防止のため、`melon_order_requests` テーブルに `notified_new_at` / `notified_overdue_at` を追加し、送信済みかどうかを記録
- 通知先は `line_recipients` テーブル（`label = 'admin_alert'`）を `check-delayed-tasks` と共用。LINE公式アカウントの友だち追加が既に済んでいれば追加設定不要
- 関連ファイル：`migration_2026-08-13b_order_request_notify.sql` / `supabase/functions/notify-order-requests/index.ts`
- **保留事項**：LINE公式アカウントの無料プランは月200通までしか送れず、タスクの遅延通知（`check-delayed-tasks`）と合算するとすぐに上限へ達する可能性がある。村上農園スタッフと相談のうえ、必要になれば注文リクエスト通知だけメール（Resend経由）に切り替える想定。ただしResendは独自ドメイン未認証のため、宛先を変更するにはその宛先でResendアカウントを作り直す必要がある（詳細は上記「通知機能（メール）」の制約と同様）

---

## スタッフ画面の多言語対応（2026-08-27追加）

- **背景**：外国人技能実習生など、日本語の読解が難しいスタッフが増えたため、`login.html`・`staff.html`の表示言語を切り替えられるようにした（`admin.html`は対象外。管理者は日本語話者のため）
- ヘッダー上部に「日本語／Indonesia／ខ្មែរ（クメール語）」の切り替えボタンを設置。選択した言語は`localStorage`の`staffLang`キーに保存し、ログイン画面・タスク画面の両方で共有される（ログイン画面で選んだ言語のままログイン後の画面も表示される）
- 実装方式：`STAFF_I18N` / `LOGIN_I18N`という言語別辞書オブジェクトを各HTMLのJS内に定義し、`st(key)`関数で現在の言語の文言を取得。静的なラベルはページ読み込み時に`applyStaffI18n()`で一括置換、動的に生成されるタスクカードやアラートメッセージも同じ辞書を参照する
- 既存の「玉ねぎ日報」機能に元々あった多言語対応（`ONION_I18N`、日本語に他言語を併記する方式）とは独立した仕組み。玉ねぎ日報はそのまま従来通り
- **注意**：インドネシア語・クメール語の翻訳文言はAIによる翻訳のため、実際に使用する前にスタッフによるネイティブチェックを推奨

---

## タスク完了時の時間帯別人数記録（2026-08-27追加）

- **背景**：これまで「完了」ボタンを押すだけで、そのタスクに実際に何人が作業したかは記録されていなかった。作業途中で人数が増減するケースもあるため、時間帯を分けて人数を記録できるようにした
- **変更内容**：スタッフ画面の「完了」ボタンを押すと、直接完了にはならず、時間帯（開始・終了、15分刻み）と人数を入力する小さなモーダルが開く。初期状態でタスクの予定時刻が1行分プリセットされており、人数だけ入力すればよい。作業途中で人数が変わった場合は「＋ 行を追加」で行を増やし、時間帯を分けて入力する（玉ねぎ日報の時間入力UIと同じ方式）
- 最低1行、開始・終了・人数がすべて入力されていないと「完了する」ボタンでは確定できない（必須入力）
- データは`task_completions.worker_time_slots`（jsonb）に`[{"start":"11:00","end":"11:45","count":3}, ...]`の形式で保存。関連migration：`migration_2026-08-27_task_worker_time_slots.sql`（Supabase SQL Editorで実行が必要）
- 管理者画面（admin.html）のCSVエクスポート（作業記録）にも「時間帯別人数」列を追加。「11:00〜11:45:3人／11:45〜12:00:2人」のようなテキスト形式で1セルにまとめて出力

---

## 勤怠管理（出勤・退勤打刻）機能（2026-08-29追加）

- **背景**：これまでタスク管理アプリとは別に、出勤・退勤の記録手段がなかった。事務所に置いたタブレットでスタッフ全員が打刻し、月末の賃金計算用に労働時間を集計できるようにしたい、という要望から追加
- **位置づけ**：既存のタスク管理・インスタEC機能とは独立した新機能。同じGitHub Pagesサイト内に新しいページ（`kintai.html`）を追加する形で実現し、別ドメイン・別ホスティングは用意していない
- **打刻ページ（`kintai.html`）**：
  - 事務所タブレット用の**共有ログインアカウント**（`profiles.role = 'kiosk'`）でログインして常時開いておく想定。ログイン不要の公開ページにはしていない（URLが漏れても第三者が打刻データを操作できないようにするため）
  - スタッフ全員の名前ボタンが並び、タップすると本人の状態（未出勤／出勤中／退勤済み）に応じて「出勤する」「退勤する」ボタンが表示される
  - 出勤前の取消・退勤前の取消が可能。操作後20秒操作がなければ自動的に名前一覧へ戻る（次の人が使いやすいように）
  - 本人識別は名前ボタンのタップのみ（PINコード等は導入していない）
- **管理画面**：admin.htmlに「⏰ 勤怠」タブを追加
  - 期間を指定して日別の勤怠一覧を表示、時刻の手動修正・記録削除が可能（打刻漏れ・押し間違い対応）
  - Excel（.xlsx、2シート構成：「日別勤怠記録」「スタッフ別集計」）でダウンロード可能。スタッフ別集計は出勤日数・合計労働時間をまとめたもので、月末の賃金計算の元データとして使う想定
  - **労働時間は出退勤の生の時間をそのまま集計**しており、休憩時間（10:00-10:30等）の自動控除は行っていない。時給×時間の金額計算も行わず、集計まで
- **バックアップ方針**：自動メール送付等の二重バックアップは導入せず、Supabase DBへの保存のみ（ユーザーとの相談の結果、DBのみで十分と判断）
- **DB設計**：新テーブル`attendance_records`（1スタッフ・1日1レコード、`clock_in`/`clock_out`）。`profiles.role`に`'kiosk'`を追加（CHECK制約更新）。関連migration：`migration_2026-08-29_kintai.sql`（Supabase SQL Editorで実行が必要）
- **セットアップ手順**：migration実行後、Supabaseダッシュボード（Authentication → Users）で事務所タブレット用の共有アカウントを作成し、`update public.profiles set role = 'kiosk' where id = '（作成したユーザーのUUID）';`でroleを変更する。ログイン後、自動的に`kintai.html`へリダイレクトされる
- `js/supabase.js`の`requireAuth`/ログイン後リダイレクト先の判定を`roleHomePage(role)`関数に共通化し、admin/staff/kioskの3ロールに対応

### 勤怠打刻対象メンバーの独立管理（2026-08-30追加）

- **背景**：勤怠管理の対象は、パート・社員・技能実習生・特定技能者を含む全従業員（R8.8時点で24名）であり、タスク管理アプリのスタッフ（`profiles`、Supabase Authの個別ログインアカウントを持つ人）とは一致しない。また打刻自体に個別ログインは不要なため、`profiles`とは別に専用マスタで管理する方式に変更
- 新テーブル`kintai_staff`（名前・分類・在留資格や雇用形態・並び順・在籍中フラグ）を新設し、`attendance_records.staff_id`の参照先を`profiles`から`kintai_staff`に変更。関連migration：`migration_2026-08-30_kintai_staff.sql`（`migration_2026-08-29_kintai.sql`の後に実行が必要）
- 初期データとして`従業員R8.8.pdf`の24名を登録済み。打刻ボタンの表示名は簡潔にするため、外国人スタッフは本人が呼ばれている名前1語（インドネシア人は名が先頭のためローマ字表記の最初の単語、カンボジア人は姓が先頭のため2番目の単語）のカタカナ表記、日本人は苗字のみを採用
- admin.htmlの「⏰ 勤怠」タブに「👥 勤怠メンバー管理」セクションを追加。名前・分類・資格を入力してメンバーを追加でき、既存メンバーは「編集」から名前変更・退職済みへの切り替え（`active=false`、打刻対象から外れるが過去記録は残る）・完全削除（`attendance_records`も連動削除されるため要注意、退職者は基本的に非アクティブ化で対応する運用を想定）ができる

### 勤怠タブのUI改善（2026-08-30追加）

- kintai.html（打刻画面）：名前ボタンを状態で色分け（出勤中＝薄緑、退勤済み＝グレー）し、一目で状況が分かるようにした
- kintai.html：出勤・退勤ボタンの押し忘れに備え、「出勤時間を選択する」「退勤時間を選択する」ボタンを追加。15分刻みの時刻セレクトで、操作している時点より前の時刻のみ選択可能（例：12:39に操作した場合、選べる最新の時刻は12:30）
- admin.htmlのExcelエクスポート「スタッフ別集計」シートに「属性」列（B列）を追加し、在留資格・雇用形態を表示。技能実習生3号は法定労働時間（1日8時間・週40時間）の制限対象のため、集計表でも区別できるようにするため
- admin.htmlの「⏰ 勤怠」タブ内のセクション順序を「勤怠管理（未出勤メンバー一覧＋日別記録）」→「勤怠管理のエクスポート」→「勤怠メンバー管理」に並び替え
- 「勤怠管理」セクションに「本日まだ出勤していないメンバー」一覧を追加し、出勤ボタンの押し忘れをその場でフォローできるようにした
